# 검증 기록

환경: Apple Silicon, macOS 26.5.2, Xcode 26.6. 2026-09-13.

- 영어 → 한국어: 실제 메뉴 막대 앱에서 회의 안내 문장 자동 번역 완료.
- 한국어 → 영어: 제목과 여러 문단을 포함한 실제 번역 완료.
- 일본어 → 한국어: 실제 앱에서 회의 안내 문장 자동 감지·번역 완료.
- HTML 제목·굵게·기울임이 포함된 원문을 앱에 붙여넣은 뒤 선택하여 ⌘C로 복사. 실제 클립보드에는 `public.utf8-plain-text`와 macOS 일반 텍스트 별칭 `NSStringPboardType`만 존재. HTML·RTF 없음, 줄바꿈 보존.
- 클립보드 불러오기 버튼이 클립보드 내용을 원문에 넣고 바로 번역하는 경로 확인. 전역 단축키도 같은 처리 함수를 사용.
- 자동 테스트 23개 통과: 입력창의 텍스트 저장소 및 선택 복사, 일반 텍스트 우선 처리, RTF 추출, 비텍스트 클립보드, 단축키 정의, 한영 전환, 요청 무효화, 동일 언어 원문 유지, 기록 설정, 길이 제한, 자동 번역 취소, 번역 완료 후 선택 요청 및 이전 요청·창 닫기·화면 전환 시 선택 취소.
- 실제 번역 엔진과 결과 편집기를 사용하는 별도 앱 테스트에서 번역 완료 후 결과 포커스·전체 선택 확인. 이후 부분 선택이 일반 화면 업데이트에도 유지됨을 확인. 다른 앱이 활성 상태이면 포커스를 가져오지 않음.
- 앱 번들 빌드 및 로컬 코드 서명 확인.
- 1.1 화면 개선: 밝은/어두운 화면의 텍스트 대비, 언어 버튼 마우스 클릭, `ja` 검색 후 Return으로 일본어 선택, 복사 버튼의 체크·안내 표시를 실제 앱에서 확인.
- 1.1에서 자동 테스트 24개 통과. 한글 문자·Caps Lock·Fn 플래그를 포함한 복사 단축키 이벤트 검증 추가. 개선된 실제 화면과 번역 엔진을 사용하는 앱 테스트에서 번역 후 전체 선택 및 화면 갱신 후 부분 선택 유지 재확인.
- 1.2에서 자동 테스트 36개 통과. 사용자 지정 조합 저장·복원, 기존 네 가지 설정 이전, 손상된 설정 복구, 단독 키·Shift 단독 거절, Esc 취소, 한글 입력 상태의 물리 키 판별, 실제 Carbon 등록 충돌 시 기존 조합 유지 확인.
- 별도 설정 검증 앱에서 ⌥⌘K를 직접 입력해 저장하고 모델을 다시 생성했을 때 유지됨을 확인. 단독 키 오류 표시와 Esc 취소 후 기존 설정 유지도 실제 화면에서 확인.

실제 키보드로 다른 앱에서 전역 단축키를 누르는 동작과 미설치 언어 모델의 다운로드 승인 화면은 자동 UI 도구로 검증하지 않았습니다. 전역 단축키는 Carbon 등록 API를 사용하고, 언어 다운로드는 Apple 공식 `translationTask`와 `NSHostingController`를 사용합니다.
